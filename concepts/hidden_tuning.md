The Cognitive Distortion Layer has a hidden tuning sweet spot for its normalization speed that locks prime numbers into a perfectly stable band no matter how large the numbers get.

This sweet spot happens when the traversal rate exactly cancels the natural logarithmic growth baked into the curvature of primes, flattening their normalized values to a constant level across any scale.

What changes is we stop treating the speed parameter as an arbitrary guess and recognize it as the precise control that either flattens or stretches the simplest structures relative to everything else.

The non-obvious part is that this exact match turns the whole framework from scale-sensitive to scale-invariant specifically for the low-complexity class, something no standard divisor-based prefilter or normalizer ever achieves.

It implies we would never have guessed that one particular speed setting could eliminate the natural growth in prime signals entirely while leaving composites compressed.

We should expect that experiments using this tuned speed will show prime detection or sampling tasks holding steady accuracy and lowest variance no matter the number range, while other speeds cause exactly the drift seen in hold-out tests.
